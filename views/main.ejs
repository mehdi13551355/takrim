<!DOCTYPE html>
<html lang="fa">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>سامانه</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        @font-face {
            font-family: 'Vazir';
            src: url('fonts/BYekan.woff2') format('woff2'),
                url('fonts/BYekan.woff') format('woff');
        }

        * {
            font-family: "Vazir", Tahoma, sans-serif;
            direction: rtl;
        }

        body {
            margin: 0;
            padding: 0;
            direction: rtl;
            box-sizing: border-box;
        }

        .container {
            display: grid;
            grid-template-areas:
                "header header"
                "sidebar main"
                "footer footer";
            align-self: auto;
            grid-template-rows: 100px 1fr 60px;
            grid-template-columns: 1fr 4fr;
            height: 100vh;

        }

        header {
            grid-area: header;
            color:white;
            background:linear-gradient(135deg, #74b0b4, #84fab0);
        }

        header h2 {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .sidebar {
            grid-area: sidebar;
            background-color: #f0f0f0;
            padding: 20px;
        }

        main {
            grid-area: main;
            background-color: #ffffff;
            padding: 5px;
            text-align: center;
        }

        footer {
            grid-area: footer;
            background:linear-gradient(135deg, #74b0b4, #84fab0);
            color: white;
            text-align: center;
            padding: 10px;
        }

        .sidebar ul {
            list-style-type: circle;
            padding: 0;
            font-size: larger;

        }

        .sidebar ul li {
            margin: 10px 0;

        }

        .sidebar ul li a {
            text-decoration: none;
            color: #333;
        }

        .sidebar ul li a:hover {
            text-decoration: underline;
        }

        .image-container {
            position: relative;
            width: 100%;
            height: 100%;
            margin: 0px auto;
            overflow: hidden;
            align-items: center;
            border-radius: 20px;
        }

        img {
            display: none;
            width: 100%;
            height: 100%;
        }

    </style>
</head>

<body>
    <div class="container">
        <header>
            <h2>سامانه تکریم ارباب رجوع </h2>
        </header>
        <div class="sidebar">
            <ul>
                <li><a href="#">رهگیری گواهی عضویت وخدمات</a></li>
                <li><a href="#"> ارتباط گیری با کارشناس</a></li>
                <li><a href="#">نوبت گیری حضوری</a></li>
                <li><a href="#">درباره ما</a></li>
            </ul>
        </div>
        <main>
            <div class="image-container">
                <img id="mainImage1" src="images/gol1.png" alt="تصویر نمونه">
                <img id="mainImage2" src="images/gol2.png" alt="تصویر نمونه">
                <img id="mainImage3" src="images/gol3.png" alt="تصویر نمونه">
                <img id="mainImage4" src="images/gol4.png" alt="تصویر نمونه">
            </div>
        </main>
        <footer>
            <p id="pfoo">تمامی حقوق محفوظ است © 1404</p>
        </footer>
    </div>
    <script>
        $(document).ready(function () {
            let images = $('.image-container img');
            let index = 0;

            function showNextImage() {
                $(images[index]).fadeOut(1000, function () {
                    index = (index + 1) % images.length;
                    $(images[index]).fadeIn(1000);
                });
            }
            // نمایش تصویر اول
            $(images[0]).show();

            // تغییر تصاویر هر 3 ثانیه
            setInterval(showNextImage, 3000);
        });
    </script>
</body>

</html>